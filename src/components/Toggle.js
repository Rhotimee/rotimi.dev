import React from "react"
import styled from "@emotion/styled"

const ToggleStyles = styled.div`
  position: absolute;
  top: 2rem;
  right: 3rem;
`

const Toggle = () => <ToggleStyles>Toggle</ToggleStyles>

export default Toggle
